# Worker Agent Environment Configuration
# Copy this to .env.worker1.local, .env.worker2.local, .env.worker3.local
# and set worker-specific values for each instance.

# ─── Worker Identity ────────────────────────────────────────────────────────
WORKER_ID=worker1
PORT=3001

# ─── Ensue Memory Network (shared coordination layer) ──────────────────────
ENSUE_API_KEY=your-ensue-api-key
ENSUE_TOKEN=your-ensue-api-key

# ─── Nova SDK (private per-worker identity storage) ─────────────────────────
# Each worker MUST have its own Nova account and group.
# Create accounts at https://nova-sdk.com
NOVA_API_KEY=nova_sk_YOUR_WORKER_KEY
NOVA_ACCOUNT_ID=your-worker-account.near
NOVA_GROUP_ID=your-worker-private-group

# ─── NEAR AI (shared, provided by coordinator) ─────────────────────────────
NEAR_AI_API_KEY=your-near-ai-api-key

# ─── NEAR Contract Configuration ───────────────────────────────────────────
NEAR_NETWORK=testnet
NEAR_RPC_JSON=https://test.rpc.fastnear.com
NEXT_PUBLIC_contractId=ac-proxy.agents-coordinator.testnet

# ─── Docker (optional) ─────────────────────────────────────────────────────
DOCKER_TAG=manza/worker-agent:latest
