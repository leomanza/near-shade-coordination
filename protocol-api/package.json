{
  "name": "protocol-api",
  "version": "1.0.0",
  "description": "Delibera Protocol API — deployment, payments, agent registry",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "DOTENV_CONFIG_PATH=.env.development.local tsx -r dotenv/config src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@hono/node-server": "^1.15.0",
    "@near-shade-coordination/shared": "file:../shared",
    "@phala/cloud": "^0.2.3",
    "dotenv": "^16.5.0",
    "hono": "^4.8.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
